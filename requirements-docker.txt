# Install after torch+torchaudio from CUDA index in Dockerfile (so we don't overwrite with CPU).
# resemble-enhance is cloned from Git in Dockerfile; patch_deepspeed_optional.py makes
# deepspeed optional in enhancer/train.py so inference works without installing deepspeed.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
supabase>=2.0.0
httpx>=0.25.0
scipy>=1.11.0
omegaconf>=2.3.0
soundfile>=0.12.1
numpy>=1.26.2
pandas>=2.1.0
tqdm>=4.65.0
tabulate>=0.9.0
librosa>=0.10.0
rich>=13.7.0
matplotlib>=3.8.0
